#include "main.h"

/**
 *print_numbers - prints numbers from 0 to 9
 *@c: parameter to be printed
 *Return: c
 */

void Print_numbers(void)
{
	int c;
	
	for (c = 0; c <= 9; c++;)
	{
		_putchar(c);
	}
	_putchar('\n');
	return (c);
}
