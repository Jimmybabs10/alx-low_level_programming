#include "main.h"
#include <string.h>

/**
 *_strlen - returns the length of a string
 *
 *@s: parameter to check for length
 *
 *Return: nothing
 *
 */

int _strlen(char *s)
{
	return (_strlen(s));
}
