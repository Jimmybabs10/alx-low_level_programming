#include "main.h"

/**
 *add - adds two integers and returns the result
 *@a: parameter one
 *@b: parameter two
 *Return: the sum of the two parameters
 */

int add(int a, int b)
{
	int sum = a + b;

	return (sum);
}
