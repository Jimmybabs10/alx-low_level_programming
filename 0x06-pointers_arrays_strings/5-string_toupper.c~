#include "main.h"

/**
 *string_touppper - changes all lowercase letters of a string to uppercase
 *@str:
 *Return: nothing
 */

char *string_toupper(char *)
{
	int i;

	for (i = 0; str[i] != '\0'; i++)
	{
		if (str[i] >= 'a' && str[i] <= 'z')
		{
			str[i] = str[i] - 32;
		}
	}
	return (str);
}
